<div class="container">
  <div class="row justify-content-center">
    <div class="col-6">
      <div class="book-card-m0 mt-5 pt-5">
        <h2>Crear <%= print.product.name %></h2>

          <%= simple_form_for([product, print]) do |f| %>
            <%= f.input :photos, as: :file, input_html: { multiple: true }, label: "Foto?" %>
            <%= f.button :submit, "Crear #{print.product.name}" , class: "btn-primary"%>
          <% end %>

      </div>
    </div>
  </div>
</div>
